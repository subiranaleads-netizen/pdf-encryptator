services:
  - type: web
    name: pdf-encryptator
    env: python
    plan: free
    buildCommand:
      rm -f pyproject.toml poetry.lock  # 🔥 elimina cualquier rastro por si acaso
      pip install --upgrade pip
      pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
